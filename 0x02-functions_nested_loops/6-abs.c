#include "main.h"

/**
 * _abs - converts an integer to absolute value
 *
 * @r: r is an ascii character
 *
 * Return: 0 (success)
 */

int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
